# Пошаговый runbook: AI-разработка с двумя раздельными репозиториями

Версия: 2.1  
Дата: 21 августа 2026 г.  
Целевая аудитория: AI-интегратор, который впервые подключает AI к клиентскому проекту.

Эта версия описывает весь жизненный цикл:

1. согласование использования AI;
2. создание и защита приватного AI-репозитория;
3. подключение готового Starter Kit к клиентскому checkout;
4. установка единой команды `aiw`;
5. подключение Codex Desktop и Claude Desktop без AI-файлов в client repo;
6. настройка Starter Kit агентом;
7. ежедневную работу команды от задачи до human-reviewed PR;
8. onboarding, offboarding, аудит и обновление;
9. controlled learning loop для улучшения skills на реальных ошибках без client artifacts.

Актуальные артефакты:

- каталог `project-ai-workspace-template` — Starter Kit;
- архив `project-ai-workspace-template.zip` — копия для нового приватного AI-repo;
- `project-ai-workspace-template/SETUP-PROJECT-WITH-AGENT-RU.md` — инструкция, по которой агент донастраивает пару репозиториев;
- `project-ai-workspace-template/DESKTOP-AND-CLI-RU.md` — короткая инструкция по CLI и Desktop.

В этом runbook основной путь — использование готового Starter Kit. Ручное создание файлов оставлено как справочный fallback и не должно смешиваться с готовой JSON-конфигурацией.

## 1. Что должно получиться в конце

Для каждого проекта существуют ровно два Git-репозитория.

### Репозиторий 1: клиентский

Условное имя: `client-product`.

Содержит:

- продуктовый код;
- тесты;
- продуктовую и эксплуатационную документацию;
- обычные спецификации и ADR, если они приняты процессом клиента.

Не содержит:

- agents, skills и prompts;
- конфигурации конкретного AI-инструмента;
- транскрипты и память агента;
- временные AI-планы;
- внутренние eval-наборы;
- подписи `Generated by AI`, AI co-author trailers и аналогичные служебные следы.

### Репозиторий 2: приватный AI-workspace проекта

Условное имя: `client-product-ai-workspace`.

Содержит:

- роли агентов;
- skills и инструкции;
- шаблоны specification, plan, tasks, review и documentation;
- project profile;
- workflow для feature, bug fix, tests и docs;
- правила доступа агента;
- launcher для запуска AI-сессии;
- сканер утечки AI-артефактов;
- обезличенные project-specific evals;
- журнал решений по настройке AI-процесса.

Не содержит:

- копию клиентского исходного кода;
- secrets;
- production data;
- персональные данные;
- полный transcript работы с клиентским кодом;
- credentials для Git, CI/CD, cloud и production.

### Как репозитории связаны

Они не связываются средствами Git. Не используются:

- Git submodule;
- Git subtree;
- вложенный Git-репозиторий;
- символическая ссылка из клиентского репозитория;
- копирование AI-файлов в клиентский checkout.

Оба репозитория клонируются в соседние локальные каталоги. Launcher из AI-репозитория получает путь к клиентскому checkout, проверяет его remote URL, запускает агента с клиентским каталогом как рабочей областью и подгружает инструкции из AI-репозитория.

Итоговая структура на компьютере разработчика:

```text
workspaces/
└── acme-billing/
    ├── client-product/               # Git remote заказчика
    ├── client-product-ai-workspace/  # приватный Git remote аутсорсера
    └── .ai-runtime/                  # временные файлы; не является Git-репозиторием
```

## 2. Обязательные решения до технической настройки

### Шаг 1. Назначить владельцев

Заполните таблицу и сохраните её во внутренней системе управления проектом.

| Ответственность | ФИО/роль |
|---|---|
| Разрешает использование AI на проекте | Delivery manager |
| Проверяет договор и confidentiality | Legal/Account manager |
| Утверждает AI-инструменты | Security/IT |
| Владеет AI-workspace | AI integrator |
| Утверждает архитектурные изменения | Tech lead клиента/проекта |
| Принимает код | Human reviewer |
| Реагирует на утечку данных | Security incident owner |

Проверка: у каждой строки есть конкретный человек, а не только название отдела.

Результат: понятно, кто принимает решение, если AI просит доступ, возникает риск или нужен exception.

### Шаг 2. Проверить договор с клиентом

Вместе с Legal/Account manager ответьте письменно:

1. Разрешено ли применять generative AI при разработке?
2. Нужно ли уведомить клиента или получить согласие?
3. Можно ли передавать исходный код стороннему AI-провайдеру?
4. Разрешены ли subprocessors и в каких странах?
5. Какие требования действуют к retention и удалению данных?
6. Есть ли специальные требования к IP, лицензиям и code provenance?
7. Какие данные запрещено передавать при любых условиях?

Если хотя бы на один критичный вопрос нет ответа, не подключайте AI к клиентскому коду. Разрешены только публичные или синтетические примеры до получения решения.

Проверка: существует датированное решение `Allowed`, `Allowed with restrictions` или `Not allowed`.

Результат: определена правовая граница использования AI.

### Шаг 3. Выбрать режим данных

Выберите один режим.

| Режим | Что разрешено |
|---|---|
| Green | публичный код, synthetic data, общие технические вопросы |
| Amber | клиентский код через утверждённый enterprise/API AI-сервис; без secrets и production data |
| Red | клиентский код нельзя отправлять внешней модели; только approved local/on-prem AI либо работа без AI |

Запишите выбранный режим в `project/profile.json`, который будет настроен далее.

Проверка: Security и Delivery manager согласовали режим.

Результат: разработчик понимает, что разрешено передавать агенту.

### Шаг 4. Выбрать AI-инструмент

Проверьте отдельно IDE chat, CLI/local agent, cloud agent, code review и MCP/plugins. Для каждой функции ответьте:

- используется ли input/output для обучения;
- каков retention;
- где обрабатываются данные;
- есть ли SSO/MFA и корпоративное управление;
- можно ли запретить отдельные модели и функции;
- можно ли ограничить filesystem, network и команды;
- доступны ли audit logs;
- можно ли отключить cloud agent и сторонние MCP;
- как выполняется удаление данных.

Настройка для первого пилота:

- используйте корпоративный аккаунт;
- отключите preview-функции;
- отключите cloud agents;
- отключите все MCP/plugins, затем разрешайте по одному;
- разрешите только утверждённые модели;
- включите минимально возможный retention;
- запретите обучение на business data;
- включите public-code matching/blocking, если функция доступна;
- запретите использование personal/free accounts.

Проверка: Security сохранил скриншоты или экспорт настроек и дату проверки.

Результат: определена управляемая поверхность AI, которую можно использовать в пилоте.

## 3. Создание приватного AI-репозитория

Выберите только одну инструкцию: GitHub, GitLab или Bitbucket. Клиентский и AI-репозитории могут находиться на разных платформах; локальная связь от этого не меняется.

Используйте имя `<client-project>-ai-workspace`, например `acme-billing-ai-workspace`. Не включайте в имя секретный codename или персональные данные.

### Вариант A. GitHub

#### Создать репозиторий через интерфейс

1. Войдите в корпоративную организацию GitHub.
2. В правом верхнем углу нажмите `+`.
3. Выберите `New repository`.
4. В поле `Owner` выберите организацию аутсорсера, не личный аккаунт.
5. В `Repository name` введите `<client-project>-ai-workspace`.
6. В `Description` укажите `Private AI delivery workspace for <project-code>. No customer source code.`
7. Выберите видимость `Private`. Не выбирайте `Internal`, потому что internal-репозиторий может быть доступен всем участникам enterprise.
8. Включите `Add a README file`.
9. Выберите корпоративный `.gitignore` для ОС или оставьте без шаблона; полный файл будет добавлен далее.
10. Нажмите `Create repository`.

Официальная последовательность создания: [GitHub Docs](https://docs.github.com/en/repositories/creating-and-managing-repositories/creating-a-new-repository).

#### Настроить доступ

1. Откройте репозиторий.
2. Перейдите `Settings` → `Collaborators and teams` или `Manage access`.
3. Добавьте команду проекта.
4. Разработчикам дайте роль `Write`.
5. AI-интегратору и одному резервному владельцу дайте `Maintain`.
6. Роль `Admin` оставьте только владельцам организации/репозитория.
7. Удалите прямой доступ людей, которым репозиторий не нужен.

Официальная справка по team access: [GitHub Docs](https://docs.github.com/en/enterprise-cloud%40latest/organizations/managing-user-access-to-your-organizations-repositories/managing-repository-roles/managing-team-access-to-an-organization-repository).

#### Защитить main

1. Перейдите `Settings` → `Rules` → `Rulesets`.
2. Создайте branch ruleset для `main`.
3. Запретите force push и deletion.
4. Включите обязательный pull request.
5. Требуйте минимум одного approval.
6. Включите dismiss stale approvals после новых commits.
7. Запретите bypass всем, кроме двух администраторов для аварийного восстановления.
8. Сохраните ruleset и переведите его в `Active`.

Проверка: обычный разработчик не может выполнить прямой push в `main`.

### Вариант B. GitLab

#### Создать project/repository

1. Войдите в корпоративную группу GitLab.
2. Вверху нажмите `Create new` → `New project/repository`.
3. Выберите `Create blank project`.
4. В `Project name` введите `<client-project>-ai-workspace`.
5. Убедитесь, что `Project URL/Namespace` указывает на корпоративную группу.
6. В `Project slug` оставьте безопасное имя без клиентских секретов.
7. В `Visibility Level` выберите `Private`.
8. Включите `Initialize repository with a README`.
9. Нажмите `Create project`.

Официальная последовательность: [GitLab Docs](https://docs.gitlab.com/user/project/).

#### Настроить участников

1. Откройте project.
2. Перейдите `Manage` → `Members`.
3. Добавьте проектную группу или конкретных участников.
4. Разработчикам назначьте `Developer`.
5. AI-интегратору назначьте `Maintainer`.
6. `Owner` оставьте владельцам верхнеуровневой группы.
7. Не задавайте access expiration `Never` внешним/временным участникам; укажите дату окончания проекта.

#### Защитить main

1. Перейдите `Settings` → `Repository`.
2. Откройте `Branch rules`.
3. Создайте или откройте правило для `main`.
4. `Allowed to push and merge` установите `No one`.
5. `Allowed to merge` установите `Maintainers` или согласованную группу.
6. Отключите force push.
7. В настройках merge requests потребуйте минимум один approval.

Официальная последовательность protection: [GitLab Docs](https://docs.gitlab.com/user/project/repository/branches/protected/).

Проверка: Developer создаёт branch и merge request, но не может push напрямую в `main`.

### Вариант C. Bitbucket Cloud

#### Создать repository

1. Войдите в корпоративный Bitbucket Workspace.
2. Нажмите `Create` → `Repository`.
3. В `Workspace/Project` выберите корпоративный workspace и проект.
4. В `Repository name` введите `<client-project>-ai-workspace`.
5. Оставьте access `Private`.
6. В `Include a README?` выберите `Yes`.
7. Нажмите `Create`.

Официальная последовательность: [Atlassian Support](https://support.atlassian.com/bitbucket-cloud/docs/create-a-repository/).

#### Настроить доступ

1. Откройте repository.
2. Нажмите `More actions (…)` → `Repository settings`.
3. Откройте `Repository permissions` или `User and group access`.
4. Проектной группе задайте `Write`.
5. AI-интегратору и резервному владельцу задайте `Admin`.
6. Остальным задайте `Read` либо удалите доступ.

Официальная справка: [Atlassian Support](https://support.atlassian.com/bitbucket-cloud/docs/grant-repository-access-to-users-and-groups/).

#### Защитить main

1. Откройте `Repository settings`.
2. В разделе `Workflow` выберите `Branch restrictions`.
3. Нажмите `Add a branch restriction`.
4. В `By name or pattern` укажите `main`.
5. Ограничьте direct write администраторами.
6. Разрешите merge только через pull request согласованной группе.
7. Запретите rewriting history и deletion.
8. Если тариф поддерживает merge checks, потребуйте approval и успешный pipeline.

Официальная справка: [Atlassian Support](https://support.atlassian.com/bitbucket-cloud/docs/use-branch-permissions/).

Проверка для любой платформы:

- репозиторий имеет видимость Private;
- владелец — корпоративная организация/workspace/group;
- доступ имеет только проектная команда;
- main защищён;
- personal fork запрещён политикой или процессом;
- включены MFA/SSO и audit там, где это поддерживает тариф.

## 4. Подготовка структуры AI-репозитория

### Основной путь: развернуть готовый Starter Kit

Шаги 5–13 ниже описывают состав AI-workspace и полезны для аудита или ручного восстановления. Для нового проекта не создавайте эти файлы вручную. Выполните следующий порядок.

#### Шаг 5A. Получить Starter Kit

Возьмите проверенный `project-ai-workspace-template.zip` из внутреннего хранилища аутсорсера либо каталог `project-ai-workspace-template` из этого комплекта.

Перед копированием:

1. проверьте checksum архива по внутреннему release record;
2. убедитесь, что версия Starter Kit утверждена Security/AI integrator;
3. не загружайте template в клиентский репозиторий;
4. не используйте общий центральный project-agent repo: у каждого проекта будет собственная приватная копия.

#### Шаг 5B. Заполнить пустой приватный AI-репозиторий

Если AI-repo уже клонирован и содержит только README:

1. распакуйте Starter Kit во временный каталог;
2. скопируйте содержимое каталога `project-ai-workspace-template`, но не сам внешний каталог, в корень AI-repo;
3. сохраните существующий `.git` AI-repo;
4. если README уже существует, объедините его содержание, а не перезаписывайте без проверки;
5. не копируйте `.DS_Store`, runtime, `.env` и архив внутрь AI-repo.

Ожидаемая рабочая структура:

```text
client-product-ai-workspace/
├── START-HERE-RU.md
├── DESKTOP-AND-CLI-RU.md
├── SETUP-PROJECT-WITH-AGENT-RU.md
├── README.md
├── package.json
├── project/
│   ├── profile.json
│   ├── permissions.json
│   ├── forbidden-artifacts.json
│   └── skill-improvement-policy.json
├── agents/
├── skills/                         feature, bug-fix, testing, docs, improvement
├── workflows/                      включая continuous-improvement
├── templates/
├── adapters/
├── evals/                          templates и synthetic behavioral cases
├── bin/
│   ├── aiw
│   ├── aiw.ps1
│   ├── aiw.mjs
│   ├── aiw-global.mjs
│   └── aiw-mcp.mjs
└── docker/
```

Проверка:

```text
node --version
node bin/aiw.mjs self-test
git status --short
```

Требуется Node.js 20 или новее. `self-test` должен вывести `PASS`.

#### Шаг 5C. Клонировать client repo рядом

Итоговые каталоги должны быть соседними:

```text
<project-parent>/
├── client-product/
├── client-product-ai-workspace/
└── .ai-runtime/                  создаётся launcher автоматически
```

macOS:

```bash
mkdir -p ~/workspaces/<project>
cd ~/workspaces/<project>
git clone <CLIENT_CLONE_URL> client-product
git clone <AI_CLONE_URL> client-product-ai-workspace
chmod +x client-product-ai-workspace/bin/aiw
```

Windows PowerShell:

```powershell
New-Item -ItemType Directory -Force C:\workspaces\<project>
Set-Location C:\workspaces\<project>
git clone <CLIENT_CLONE_URL> client-product
git clone <AI_CLONE_URL> client-product-ai-workspace
```

Если AI-repo уже был заполнен локально, повторно его не клонируйте. Разместите client checkout рядом и проверьте оба remote.

#### Шаг 5D. Настроить `project/profile.json`

Starter Kit использует JSON, а не YAML. Заполните:

1. `project.id` — безопасный slug без персональных и секретных данных;
2. `project.displayName` — внутреннее отображаемое имя;
3. `targetRepository.localRelativePath` — путь от AI-repo к client repo, обычно `../client-product`;
4. `targetRepository.runtimeRelativePath` — обычно `../.ai-runtime`;
5. `targetRepository.allowedRemotes` — точные SSH и/или HTTPS URL client origin;
6. `targetRepository.defaultBranch` — `main`, `master`, `develop` или реальная ветка;
7. `dataPolicy.lane` — только согласованное `green`, `amber` или `red`;
8. `ai.defaultTool` — `codex` или `claude`;
9. `ai.codex.model` и `ai.claude.model` — утверждённая модель либо пустая строка для enterprise default;
10. `projectCommands` — реальные команды из документации/CI клиента.

Получить client origin:

```text
git -C ../client-product remote get-url origin
```

Не используйте wildcard в `allowedRemotes`. `UNRESOLVED` разрешён во время анализа, но должен считаться blocker перед автономной реализацией.

#### Шаг 5E. Проверить permissions и delivery hygiene

Откройте:

- `project/permissions.json`;
- `project/forbidden-artifacts.json`;
- `project/skill-improvement-policy.json`;
- `docs/base-instructions.md`;
- `adapters/claude/settings.json`;
- `adapters/codex/README.md`.

Подтвердите:

- агент пишет только в client working tree и внешний runtime;
- commit, push, merge, deploy, destructive Git и production access запрещены или требуют человека;
- network отключён по умолчанию для generated commands;
- `.git`, CI settings и IDE AI settings не меняются;
- scanner запрещает `AGENTS.md`, `CLAUDE.md`, `.codex`, `.claude`, prompts, transcripts и AI attribution;
- Claude attribution и auto-memory отключены внешним adapter;
- client-specific исключения добавляются только после письменного решения.

JSON policy описывает намерение, но не является sandbox. Реальное ограничение обеспечивает Codex/Claude permission mode либо Docker.

#### Шаг 5F. Донастроить проект вручную или агентом

Ручной вариант: заполните файлы по шагам 5D–5E, затем выполните проверки ниже.

Агентный вариант:

1. откройте AI-repo в Codex или Claude Code;
2. убедитесь, что client repo находится рядом;
3. передайте агенту файл `SETUP-PROJECT-WITH-AGENT-RU.md`;
4. используйте запрос:

```text
Прочитай SETUP-PROJECT-WITH-AGENT-RU.md полностью и настрой этот AI-workspace
для соседнего клиентского репозитория. Клиентский репозиторий не изменяй.
Сначала выполни read-only preflight и задай только блокирующие вопросы.
```

Агент обязан сначала определить paths/remotes/stack/commands read-only способом, затем спросить data lane, approved tool/model и только после этого менять AI-repo. Он не получает разрешение на commit/push и не должен изменять tracked client files.

#### Шаг 5G. Установить единую команду и зарегистрировать пару

Из AI-repo на macOS или Windows PowerShell:

```text
npm install -g .
aiw register .
aiw projects
aiw self-test
aiw doctor --tool codex --mode native --require-commands
aiw install-hooks
```

Для Claude замените `codex` на `claude`. Для обязательного Docker mode замените `native` на `docker` после сборки и login.

Что происходит:

1. `npm install -g .` устанавливает короткую команду `aiw`;
2. `aiw register .` записывает пару путей в локальный `~/.aiw/projects.json`;
3. `aiw` определяет проект по текущему каталогу client repo или AI-repo;
4. `doctor` проверяет remote allowlist, Git, tool, role и data lane;
5. `install-hooks` добавляет локальный managed pre-push hook в client `.git`;
6. ни один из этих локальных файлов не становится tracked client artifact.

Для следующего проекта повторяются `npm install -g .` при обновлении версии и `aiw register <path-to-project-ai-repo>`. Список проектов показывает `aiw projects`; default меняет `aiw use <project-id>`; при неоднозначности используется `--project <project-id>`.

Если global installation запрещена:

- macOS: `./bin/aiw ...` из AI-repo;
- Windows: `.\bin\aiw.ps1 ...` из AI-repo.

#### Шаг 5H. Выполнить отрицательный тест

1. Убедитесь, что в client root нет пользовательского `AGENTS.md`.
2. Создайте временный тестовый `AGENTS.md`.
3. Запустите `aiw verify` — ожидается `BLOCK`.
4. Удалите только созданный тестовый файл.
5. Запустите `aiw verify` — ожидается `PASS`.

Если файл существовал заранее, не перезаписывайте и не удаляйте его. Остановитесь и согласуйте исключение или очистку с владельцем client repo.

### Справочный fallback: ручная сборка без Starter Kit

Следующие шаги 5–13 относятся к ручной реализации и используют концептуальные YAML-примеры. Не создавайте параллельно `profile.yaml` и `profile.json`. Если используется готовый Starter Kit, источником истины являются JSON-файлы и этот fallback следует пропустить.

### Шаг 5. Клонировать пустой AI-репозиторий

Создайте общий каталог проекта. Пример для macOS/Linux:

```bash
mkdir -p ~/workspaces/acme-billing
cd ~/workspaces/acme-billing
git clone <AI_REPOSITORY_CLONE_URL> client-product-ai-workspace
```

На Windows PowerShell:

```powershell
New-Item -ItemType Directory -Force C:\workspaces\acme-billing
Set-Location C:\workspaces\acme-billing
git clone <AI_REPOSITORY_CLONE_URL> client-product-ai-workspace
```

Замените `<AI_REPOSITORY_CLONE_URL>` на SSH или HTTPS URL из кнопки `Code/Clone` созданного репозитория.

Проверка:

```bash
cd client-product-ai-workspace
git remote -v
git status
```

Результат: локальный AI-репозиторий связан только со своим приватным remote.

### Шаг 6. Создать каталоги AI-workspace

Внутри `client-product-ai-workspace` создайте:

```text
client-product-ai-workspace/
├── README.md
├── .gitignore
├── project/
│   ├── profile.yaml
│   ├── permissions.yaml
│   ├── forbidden-artifacts.yaml
│   └── glossary.md
├── agents/
│   ├── analyst.md
│   ├── architect.md
│   ├── developer.md
│   ├── qa.md
│   ├── reviewer.md
│   └── technical-writer.md
├── skills/
│   ├── feature-flow/
│   │   └── SKILL.md
│   ├── bug-fix/
│   │   └── SKILL.md
│   ├── testing/
│   │   └── SKILL.md
│   └── documentation/
│       └── SKILL.md
├── templates/
│   ├── specification.md
│   ├── technical-plan.md
│   ├── task.md
│   ├── review-checklist.md
│   └── session-summary.md
├── workflows/
│   ├── feature.md
│   ├── bug-fix.md
│   ├── tests.md
│   └── documentation.md
├── bin/
│   ├── aiw
│   ├── verify-target
│   ├── scan-client-diff
│   └── clean-runtime
├── adapters/
│   ├── README.md
│   └── <selected-ai-tool>/
├── evals/
│   ├── README.md
│   └── golden-tasks/
├── decisions/
└── docs/
    ├── onboarding.md
    ├── incident-response.md
    └── operating-rules.md
```

Создать каталоги можно командами:

```bash
cd ~/workspaces/acme-billing/client-product-ai-workspace
mkdir -p project agents skills/{feature-flow,bug-fix,testing,documentation} templates workflows bin adapters evals/golden-tasks decisions docs
touch project/profile.yaml project/permissions.yaml project/forbidden-artifacts.yaml project/glossary.md
touch agents/{analyst,architect,developer,qa,reviewer,technical-writer}.md
touch skills/{feature-flow,bug-fix,testing,documentation}/SKILL.md
touch templates/{specification,technical-plan,task,review-checklist,session-summary}.md
touch workflows/{feature,bug-fix,tests,documentation}.md
touch docs/{onboarding,incident-response,operating-rules}.md
```

На Windows можно создать те же каталоги вручную через Explorer или PowerShell. Имена и вложенность должны совпасть.

Проверка:

```bash
git status --short
```

Результат: Git показывает только новые файлы внутри AI-репозитория.

### Шаг 7. Настроить `.gitignore` AI-репозитория

Добавьте:

```gitignore
# Ephemeral runtime
.ai-runtime/
runtime/
sessions/
transcripts/
logs/

# Secrets and local overrides
.env
.env.*
*.key
*.pem
*.p12
secrets.*
project/profile.local.yaml

# Customer source must never be copied here
client-repo/
customer-repo/
source-mirror/

# IDE/OS
.DS_Store
Thumbs.db
.idea/
```

Проверка: создайте временно файл `.env`, выполните `git status --short`; `.env` не должен появиться. Затем удалите локальный `.env`.

Результат: типичные runtime/secrets не предлагаются к commit. Это дополнительная защита, а не замена secret scanner.

### Шаг 8. Заполнить `project/profile.yaml`

Используйте следующий шаблон:

```yaml
schema_version: 1

project:
  id: acme-billing
  display_name: Acme Billing

target_repository:
  local_relative_path: ../client-product
  allowed_remotes:
    - git@github.com:customer-org/client-product.git
    - https://github.com/customer-org/client-product.git
  default_branch: main

data_policy:
  lane: amber
  allow_source_code: true
  allow_test_data: synthetic_only
  deny:
    - secrets
    - production_data
    - personal_data
    - customer_credentials
    - vulnerability_reports_not_in_task_scope

ai:
  approved_tool: REPLACE_WITH_APPROVED_TOOL
  approved_models:
    - REPLACE_WITH_APPROVED_MODEL
  cloud_agent: false
  network_access: false
  mcp_allowlist: []

project_commands:
  install: REPLACE_WITH_PROJECT_INSTALL_COMMAND
  lint: REPLACE_WITH_PROJECT_LINT_COMMAND
  typecheck: REPLACE_WITH_PROJECT_TYPECHECK_COMMAND
  test_targeted: REPLACE_WITH_PROJECT_TARGETED_TEST_COMMAND
  test_full: REPLACE_WITH_PROJECT_FULL_TEST_COMMAND
  build: REPLACE_WITH_PROJECT_BUILD_COMMAND

human_gates:
  required_for:
    - architecture_change
    - new_dependency
    - database_migration
    - authentication_or_authorization
    - external_api_change
    - destructive_command
    - push
    - merge
    - deployment
```

Как заполнить:

1. Возьмите clone URL клиентского репозитория и внесите SSH/HTTPS варианты в `allowed_remotes`.
2. Не используйте wildcard вроде `*customer*`.
3. Укажите реальную default branch.
4. Укажите согласованный Green/Amber/Red lane.
5. Возьмите команды install/lint/test/build из README и CI клиентского репозитория.
6. Если команды пока неизвестны, напишите `UNRESOLVED` и не разрешайте автономную реализацию.

Проверка: Tech lead подтверждает команды и remote URL.

Результат: launcher сможет отличить правильный клиентский checkout от случайного каталога.

### Шаг 9. Заполнить `project/permissions.yaml`

Начальный безопасный профиль:

```yaml
filesystem:
  read:
    - ../client-product
    - ./agents
    - ./skills
    - ./templates
    - ./workflows
    - ./project
  write:
    - ../client-product
    - ../.ai-runtime
  deny_write:
    - ../client-product/.git
    - ../client-product/.github
    - ../client-product/.gitlab
    - ../client-product/.idea
    - ../client-product/.vscode

commands:
  allow_categories:
    - inspect_files
    - format
    - lint
    - typecheck
    - test
    - build
  require_human_confirmation:
    - install_dependency
    - change_git_config
    - commit
    - push
    - network
  deny:
    - merge
    - deploy
    - access_production
    - change_iam
    - read_user_home_secrets

network:
  default: deny
  allow: []
```

Важно: YAML сам по себе не создаёт sandbox. Adapter выбранного AI-инструмента обязан преобразовать правила в реальные tool permissions, container policy или confirmation gates. Если инструмент не умеет ограничивать права, запускайте его в dev container/VM без secrets и production access.

Проверка: попытка записи в `.git` или выполнения push требует отказа/подтверждения.

Результат: заданы правила least privilege.

### Шаг 10. Настроить список запрещённых артефактов

В `project/forbidden-artifacts.yaml` внесите:

```yaml
deny_paths:
  - AGENTS.md
  - CLAUDE.md
  - .claude/**
  - .cursor/**
  - .specify/**
  - .codex/**
  - .ai/**
  - prompts/**
  - transcripts/**
  - sessions/**
  - "*.chatlog"
  - "*.prompt.md"

deny_commit_patterns:
  - "Generated by AI"
  - "Generated with"
  - "Co-authored-by:.*(AI|Copilot|Claude|Codex|ChatGPT)"

allow_paths: []
```

Адаптируйте список под выбранный инструмент. Не запрещайте обычное слово `AI` во всём коде: оно может быть частью клиентского продукта.

Проверка: положите тестовый `AGENTS.md` в клиентский checkout и убедитесь, что будущий scanner его обнаруживает; затем удалите файл.

Результат: определена контролируемая политика чистоты клиентского репозитория.

### Шаг 11. Описать агентов

Для каждого `agents/*.md` используйте одинаковую структуру:

```markdown
# Role
[Название роли]

## Objective
[Один измеримый результат роли]

## Inputs
- approved task/specification
- project profile
- relevant customer repository files

## Required procedure
1. Read project profile and applicable workflow.
2. Inspect existing client patterns before proposing changes.
3. State unresolved assumptions.
4. Work in small, reviewable slices.
5. Run required verification.

## Must not
- invent business requirements;
- access denied data;
- add AI artifacts to the client repository;
- push, merge or deploy;
- approve its own result.

## Output contract
[Точный список результата роли]

## Human gate
[Когда остановиться и запросить решение]
```

Распределение ролей:

- `analyst`: превращает заявку в specification и список вопросов;
- `architect`: готовит technical plan, boundaries, risks и ADR draft;
- `developer`: реализует только утверждённый slice;
- `qa`: создаёт risk-based test model и проверяет acceptance criteria;
- `reviewer`: сравнивает spec, diff и tests, не исправляя молча код;
- `technical-writer`: обновляет только проверяемую документацию.

Проверка: у каждого агента есть `Must not`, output contract и human gate.

Результат: агентам назначены узкие, несовмещённые роли.

### Шаг 12. Создать workflow-файлы

В `workflows/feature.md` запишите:

```markdown
# Feature workflow

1. Intake: получить ID задачи и исходное описание.
2. Discover: прочитать только релевантные файлы и существующие тесты.
3. Specify: заполнить template specification.
4. Human gate: PO/analyst утверждает поведение и scope.
5. Plan: заполнить technical plan, включая риски и verification.
6. Human gate: tech lead утверждает значимые решения.
7. Tasks: разбить на вертикальные slices по 1–5 файлов.
8. Implement: выполнить один slice.
9. Verify: lint, typecheck, targeted tests, security checks по риску.
10. Review: отдельная роль reviewer сравнивает spec ↔ diff ↔ tests.
11. Clean: выполнить scan-client-diff.
12. Human gate: человек создаёт commit/push/PR.
13. Record: сохранить обезличенный session summary в AI-repo.
```

Аналогично создайте:

- `bug-fix.md`: reproduce → failing test → root cause → minimal patch → regression;
- `tests.md`: risk model → oracle → cases → test → prove test detects failure;
- `documentation.md`: sources → draft → execute examples → fact check → approval.

Проверка: каждый workflow содержит вход, результат, verification и human gates.

Результат: работа выполняется воспроизводимо, а не произвольным prompting.

### Шаг 13. Зафиксировать первую версию AI-repo

```bash
git checkout -b setup/initial-ai-workspace
git add README.md .gitignore project agents skills templates workflows adapters evals decisions docs
git commit -m "Set up project AI workspace"
git push -u origin setup/initial-ai-workspace
```

Создайте pull/merge request, назначьте AI-интегратора и Security/Tech lead reviewers, затем объедините после approval.

Проверка: main содержит структуру, profile заполнен, secrets отсутствуют.

Результат: существует первая управляемая версия проектных инструкций.

## 5. Клонирование и локальная связь двух репозиториев

### Шаг 14. Клонировать клиентский репозиторий рядом

Перейдите в родительский каталог, не внутрь AI-repo:

```bash
cd ~/workspaces/acme-billing
git clone <CLIENT_REPOSITORY_CLONE_URL> client-product
```

Проверьте структуру:

```bash
pwd
ls
```

Ожидаются два соседних каталога:

```text
client-product
client-product-ai-workspace
```

Запрещено клонировать AI-repo внутрь `client-product`.

Проверка клиентского remote:

```bash
cd ~/workspaces/acme-billing/client-product
git remote get-url origin
```

Сравните вывод с `allowedRemotes` в `project/profile.json`.

Результат: клиентский код и AI-настройки физически разделены.

### Шаг 15. Создать временный runtime

```bash
cd ~/workspaces/acme-billing
mkdir -p .ai-runtime
chmod 700 .ai-runtime
```

На Windows создайте `C:\workspaces\acme-billing\.ai-runtime` и ограничьте доступ текущим пользователем через свойства Security или корпоративную endpoint policy.

Runtime используется для:

- rendered instructions;
- временных планов;
- session state;
- tool-specific config;
- локальных logs без содержимого исходного кода.

Runtime не коммитится ни в один репозиторий и удаляется после завершения задачи согласно retention policy.

Проверка: `.ai-runtime` находится на одном уровне с обоими репозиториями.

Результат: у агента есть временная рабочая область вне поставки.

### Шаг 16. Реализовать launcher `bin/aiw`

Launcher — единственная утверждённая точка запуска AI на проекте. Его нужно реализовать один раз под выбранный AI-инструмент.

Обязательная последовательность launcher:

1. Найти корень AI-repo по расположению `bin/aiw`.
2. Прочитать `project/profile.json`.
3. Разрешить `target_repository.local_relative_path` относительно AI-repo.
4. Получить фактический `git remote get-url origin` клиентского checkout.
5. Сравнить URL с `allowed_remotes` точным сравнением после нормализации SSH/HTTPS.
6. Убедиться, что AI-repo не находится внутри client-repo и наоборот.
7. Получить исходный `git status --porcelain` и сохранить только список изменённых путей в runtime.
8. Создать уникальный session directory в `.ai-runtime`.
9. Собрать инструкции из `project/`, выбранного `agents/`, `skills/` и `workflows/`.
10. Передать их выбранному AI tool через поддерживаемый внешний config/system-instruction mechanism.
11. Запустить tool с client-repo как working directory.
12. Применить реальные filesystem/network/command restrictions из `permissions.json`.
13. После завершения запустить `scan-client-diff`.
14. Показать человеку список изменённых клиентских файлов и результаты проверок.
15. Не выполнять commit, push, merge или deploy.

Интерфейс launcher должен быть одинаковым независимо от tool adapter:

```bash
./bin/aiw doctor
./bin/aiw start --role analyst --task ACME-1234
./bin/aiw start --role developer --task ACME-1234
./bin/aiw verify --task ACME-1234
./bin/aiw finish --task ACME-1234
```

Что делает каждая команда:

- `doctor`: проверяет Git, tool, profile, remotes, permissions и команды проекта;
- `start`: создаёт session и запускает выбранную роль;
- `verify`: выполняет проверки проекта и artifact scan;
- `finish`: создаёт очищенный session summary и удаляет runtime.

Если AI-инструмент требует положить `AGENTS.md`, `CLAUDE.md`, `.cursor` или другой файл непосредственно в client-repo, выберите один из вариантов:

1. используйте внешний user/workspace config инструмента;
2. adapter временно создаёт файл, но launcher гарантированно удаляет его и scanner блокирует finish при наличии файла;
3. смените инструмент;
4. согласуйте такой файл с клиентом как поставляемый артефакт.

Предпочтителен вариант 1. Вариант 2 допустим только в изолированной локальной сессии и не защищает от аварийного завершения без дополнительного cleanup hook.

Проверка `doctor` должна завершаться ненулевым exit code при:

- неверном remote;
- отсутствии client-repo;
- `UNRESOLVED` в обязательной команде;
- client-repo внутри AI-repo;
- неподдерживаемом tool или неустановленном approved CLI;
- незаполненном data lane.

Результат: связь двух репозиториев выполняется контролируемым локальным процессом.

### Шаг 17. Дополнительно создать multi-root workspace

Если команда использует VS Code-совместимый редактор, сохраните файл `client-product.code-workspace` только в AI-repo:

```json
{
  "folders": [
    { "name": "AI Workspace", "path": "." },
    { "name": "Client Product", "path": "../client-product" }
  ],
  "settings": {
    "files.exclude": {
      "../.ai-runtime": true
    },
    "search.exclude": {
      "../.ai-runtime": true
    }
  }
}
```

Открывайте его из AI-repo:

```bash
code client-product.code-workspace
```

Multi-root workspace создаёт удобное отображение, но не является security boundary. Launcher и sandbox всё равно обязательны.

Проверка: файл существует только в AI-repo; в `client-product` не появилось `.code-workspace` или `.vscode`.

Результат: разработчик видит оба каталога, но коммитит их в разные remotes.

## 6. Реализация защиты от попадания AI-файлов клиенту

### Шаг 18. Реализовать `bin/scan-client-diff`

Сканер получает путь client-repo из profile и проверяет:

1. untracked files;
2. staged files;
3. diff относительно default branch;
4. commit messages локальной ветки относительно default branch;
5. файлы, попадающие в build/package artifact, если применяется упаковка.

Алгоритм:

1. Выполнить `git status --porcelain`.
2. Получить список путей через `git diff --name-only` и `git ls-files --others --exclude-standard`.
3. Сопоставить пути с `deny_paths`.
4. Проверить текстовые diff и commit messages по `deny_commit_patterns`.
5. Выполнить штатный secret scanner проекта.
6. Выдать отчёт `PASS`, `BLOCK` или `NEEDS_REVIEW`.
7. При `BLOCK` завершиться ненулевым exit code.

Не удаляйте найденные файлы автоматически: они могут содержать работу пользователя. Покажите точные пути и инструкцию по исправлению.

Проверка:

1. Создайте в client-repo тестовый `AGENTS.md`.
2. Запустите `./bin/aiw verify` из AI-repo.
3. Ожидайте `BLOCK`.
4. Удалите тестовый файл.
5. Повторите проверку; ожидайте `PASS`.

Результат: случайный AI-артефакт блокирует завершение сессии.

### Шаг 19. Установить локальный pre-push hook

Launcher `doctor` должен предложить установить hook в `.git/hooks/pre-push` клиентского checkout. Hook не коммитится и вызывает scanner из AI-repo.

Требования:

- хранить в hook абсолютный путь к конкретному AI-repo;
- перед push выполнять `scan-client-diff`;
- при `BLOCK` отменять push;
- не модифицировать client-repo;
- уметь обновляться командой `./bin/aiw install-hooks`;
- проверять checksum/version hook в `doctor`.

Ограничение: локальный hook можно удалить или обойти. Для высокой гарантии добавьте внешний required status check, который установлен как GitHub App/GitLab integration/Bitbucket app и нейтрально называется `repository-hygiene`. Он проверяет PR diff без хранения client source. Установка такой интеграции требует разрешения владельца клиентского репозитория.

Проверка: тестовый запрещённый файл блокирует push.

Результат: ошибка ловится до отправки ветки клиенту.

### Шаг 20. Проверить отсутствие Git-связи

В client-repo выполните:

```bash
git submodule status
git ls-files | grep -E '(^|/)(AGENTS\.md|CLAUDE\.md|\.claude|\.cursor|\.specify|\.codex)(/|$)' || true
git remote -v
```

Ожидается:

- нет AI-submodule;
- нет AI-файлов;
- remote указывает только на клиента.

В AI-repo выполните:

```bash
git remote -v
git ls-files | grep -E '(^|/)(client-repo|customer-repo|source-mirror)(/|$)' || true
```

Ожидается:

- remote указывает только на приватный AI-репозиторий;
- копии клиентского кода не отслеживаются.

Результат: два репозитория разделены на уровне Git history и remotes.

## 7. Подключение терминала, Codex Desktop и Claude Desktop

Все три интерфейса используют один источник правил: приватный AI-repo. В client repo не копируются инструкции или tool settings.

```text
                         приватный AI-repo
                   profile + roles + workflows
                              │
                         aiw context/verify
                     ┌────────┼────────┐
                     │        │        │
                 Terminal   Codex    Claude
                  aiw CLI   skill   local MCP
                     │        │        │
                     └────────┼────────┘
                              │
                       client checkout
                    обычный diff без AI-файлов
```

### Шаг 21A. Работа из терминала

Откройте client repo. Переходить в AI-repo для ежедневной работы не нужно:

```text
cd <client-repo>
aiw task ACME-1234
```

Короткая команда использует значения по умолчанию:

- tool — `ai.defaultTool` из profile;
- role — `developer`;
- workflow — `feature`;
- mode — `native`.

Полный вариант:

```text
aiw task ACME-1234 --tool codex --role developer --workflow feature --mode native
```

Поддерживаемые роли: `analyst`, `architect`, `qa`, `developer`, `reviewer`, `technical-writer`.

Поддерживаемые workflows: `feature`, `bug-fix`, `testing`, `documentation`.

Полезные команды:

```text
aiw context ACME-1234 --role analyst --workflow feature
aiw doctor --tool codex --mode native
aiw verify
aiw finish ACME-1234
aiw projects
aiw use <project-id>
```

`context` печатает собранные внешние инструкции без запуска AI tool. Это используется Desktop-интеграциями и подходит для диагностики.

### Шаг 21B. Подключить Codex Desktop

Один раз для каждого проекта:

```text
cd <client-repo>
aiw desktop-install codex
```

Команда создаёт пользовательский skill в `~/.agents/skills/aiw-<project-id>/`. Skill не попадает в client repo и требует:

Codex поддерживает пользовательские skills из `.agents/skills` и их использование в Codex app/CLI; актуальная структура описана в [официальной документации OpenAI](https://learn.chatgpt.com/docs/build-skills).

1. вызвать `aiw context` до планирования или изменения кода;
2. работать только в зарегистрированном client repo;
3. не создавать AI-файлы в client repo;
4. не выполнять commit/push/merge/deploy;
5. вызвать `aiw verify` перед завершением.

Работа команды:

1. открыть client repo как workspace Codex Desktop;
2. создать или открыть клиентскую feature branch;
3. написать:

```text
Используй $aiw-<project-id> для задачи ACME-1234.
Роль developer, workflow feature. Сначала загрузи project context.
Перед завершением выполни AIW verification.
```

4. проверить предложенный plan;
5. разрешать команды только в рамках проекта;
6. после работы самостоятельно проверить diff;
7. commit/push выполняет человек.

После изменения AI-repo обновите launcher и skill:

```text
git pull --ff-only
npm install -g .
aiw register .
aiw desktop-install codex
```

### Шаг 21C. Подключить Claude Desktop

Сгенерируйте project-specific local MCP config:

```text
cd <client-repo>
aiw desktop-config claude
```

Команда печатает JSON с сервером `aiw-<project-id>`. Не заменяйте существующий `mcpServers`: объедините новый элемент с существующими настройками либо установите подготовленное корпоративное Desktop Extension.

Claude Desktop поддерживает локальные MCP servers и приватно распространяемые Desktop Extensions; порядок установки и проверки connector описан в [официальной документации Anthropic](https://support.claude.com/en/articles/10949351-getting-started-with-local-mcp-servers-on-claude-desktop).

Локальный AIW MCP предоставляет только:

- `aiw_context` — получить role/workflow/policy для task;
- `aiw_project_status` — показать paths и краткий Git status;
- `aiw_verify` — проверить diff на запрещённые артефакты.

Он не предоставляет произвольный shell, commit/push или доступ ко всему домашнему каталогу.

Пошагово:

1. добавьте сгенерированную конфигурацию после human review;
2. перезапустите Claude Desktop;
3. нажмите `+` возле поля ввода → `Connectors`;
4. убедитесь, что виден `aiw-<project-id>` и три инструмента;
5. откройте client repo через Claude Code for Desktop или разрешённую workspace-папку;
6. напишите:

```text
Вызови aiw_context для ACME-1234 с role developer и workflow feature.
Следуй полученным ограничениям. Перед завершением вызови aiw_verify.
Не выполняй commit, push, merge или deploy.
```

7. подтверждайте только ожидаемые filesystem/tool actions;
8. человек проверяет итоговый diff и выполняет Git delivery.

Обычный Claude chat без разрешённой workspace/Claude Code может получить контекст и проверить status, но не должен считаться средой редактирования client repo.

Для масштабирования на команду AI-integrator может упаковать `aiw-mcp.mjs` как приватный `.mcpb` Desktop Extension. Перед распространением нужны security review, versioning, checksum и enterprise allowlist.

### Шаг 21D. Docker mode

Используйте Docker, если native permission model недостаточен или этого требует Security.

Один раз:

```text
aiw docker-build
aiw docker-login --tool codex
aiw docker-login --tool claude
```

Запуск:

```text
aiw task ACME-1234 --tool codex --role developer --workflow feature --mode docker
```

Контейнер получает client checkout read/write, AI-repo и session instructions read-only, отдельный named volume для авторизации и не получает mount домашнего каталога. Docker не является domain-level egress allowlist; при необходимости используйте корпоративный proxy/firewall.

### Шаг 21E. Выбрать интерфейс

| Сценарий | Рекомендуемый интерфейс |
|---|---|
| Повторяемая coding-сессия из terminal | `aiw task` |
| Визуальная работа в Codex Desktop | project-specific Codex skill |
| Визуальная работа в Claude Desktop | Claude Code for Desktop + local AIW MCP |
| Строгая host isolation | `aiw task --mode docker` |
| Диагностика без запуска модели | `aiw context`, `aiw doctor`, `aiw verify` |
| Global install запрещён | `./bin/aiw` или `.\bin\aiw.ps1` |

Независимо от интерфейса остаются обязательными specification/plan, human gates, targeted verification, artifact scan и human review.

## 8. Пошаговое выполнение первой feature

### Шаг 21. Подготовить заявку

В клиентской task-системе должен быть ID и исходное описание. Не копируйте confidential ticket целиком в AI-repo. В session используйте ID и разрешённый контекст.

Перед началом заполните:

- задача: `ACME-1234`;
- человеческий owner;
- ожидаемый результат;
- out of scope;
- data lane;
- уровень процесса: Fast, Standard или High-risk.

Проверка: owner подтверждает, что задача Ready.

### Шаг 22. Проверить окружение

```bash
cd ~/workspaces/acme-billing/client-product-ai-workspace
git pull --ff-only
aiw doctor
```

Затем обновите client-repo:

```bash
cd ../client-product
git fetch --all --prune
git checkout main
git pull --ff-only
git status
```

Если есть незакоммиченные пользовательские изменения, не удаляйте их. Создайте отдельный Git worktree или попросите владельца завершить текущую работу.

Результат: обе рабочие копии актуальны, target проверен.

### Шаг 23. Создать клиентскую feature branch

```bash
cd ~/workspaces/acme-billing/client-product
git checkout -b feature/ACME-1234-short-description
```

Если процесс клиента задаёт иной шаблон branch name, используйте его.

Проверка:

```bash
git branch --show-current
```

Результат: изменения изолированы от main.

### Шаг 24. Запустить analyst

```bash
cd ../client-product
aiw task ACME-1234 --role analyst --workflow feature
```

Analyst должен выдать draft по `templates/specification.md`:

- problem/outcome;
- scope/out of scope;
- acceptance criteria;
- examples и edge cases;
- non-functional requirements;
- open questions;
- источники каждого факта.

Человек отвечает на вопросы и утверждает specification. До утверждения developer не запускается.

Где хранить specification:

- если клиент уже хранит specs в своём repo/tracker — сохранить в принятом клиентском месте;
- если spec является только внутренним рабочим материалом — сохранить в session runtime;
- не помещать internal draft в client-repo только ради AI.

Результат: существует утверждённый testable contract поведения.

### Шаг 25. Запустить architect

```bash
aiw task ACME-1234 --role architect --workflow feature
```

Architect должен:

1. найти существующие паттерны;
2. перечислить затрагиваемые компоненты;
3. описать data flow и trust boundaries;
4. предложить минимальное решение и альтернативы;
5. определить migrations, compatibility, observability и rollback;
6. определить test strategy;
7. разбить работу на вертикальные slices по 1–5 файлов.

Tech lead утверждает план. Для auth, payments, PII, public API, database migration и infrastructure обязателен High-risk review.

Результат: developer получает ограниченный план, а не общую просьбу «сделать feature».

### Шаг 26. Запустить QA до реализации

```bash
aiw task ACME-1234 --role qa --workflow feature
```

QA формирует независимый test oracle:

- positive cases;
- negative cases;
- boundaries;
- permissions/security cases;
- regression areas;
- доказательство, что тест способен упасть при дефекте.

Не показывайте QA финальную реализацию до фиксации основных сценариев, если это практически возможно.

Результат: тесты не просто повторяют код, который позже сгенерирует developer.

### Шаг 27. Реализовать первый slice

```bash
aiw task ACME-1234 --role developer --workflow feature
```

Developer-agent обязан:

1. реализовать только первый утверждённый slice;
2. не менять unrelated files;
3. не добавлять dependency без human gate;
4. выполнить formatter/lint/typecheck/targeted test;
5. остановиться при неясном бизнес- или security-решении;
6. показать diff и выполненные проверки.

Человек просматривает diff до следующего slice.

Результат: маленькое проверяемое изменение, которое можно понять без AI.

### Шаг 28. Выполнить verification

Сначала выполните подтверждённые команды из `project/profile.json` самостоятельно или поручите агенту выполнить их по одной:

1. format/lint;
2. typecheck;
3. targeted tests;
4. integration/contract tests по затронутому flow;
5. build;
6. SAST/SCA/secret/license scan по правилам клиента;
7. ручная проверка acceptance criteria.

После технических проверок отдельно запустите delivery hygiene:

```bash
aiw verify
```

`aiw verify` проверяет изменённые/untracked пути, добавленные строки и commit messages на запрещённые AI-артефакты. Он не подменяет lint, tests, build, SAST, SCA или secret scanning.

Нельзя писать в PR `all tests passed`, если запускалась только targeted subset. Укажите точные проверки.

Результат: получен проверяемый verification report.

### Шаг 29. Выполнить независимый review

```bash
aiw task ACME-1234 --role reviewer --workflow feature
```

Reviewer сравнивает:

```text
specification ↔ technical plan ↔ actual diff ↔ tests ↔ documentation
```

Проверяет:

- scope creep;
- invented API/config;
- auth/authz и validation;
- error handling и logging;
- race conditions и edge cases;
- излишнюю сложность;
- public-code/license risk;
- соответствие проектным паттернам;
- отсутствие AI-артефактов.

После AI-review обязательный human reviewer читает код. Агент не может утверждать собственный результат.

Результат: замечания исправлены или явно приняты человеком.

### Шаг 30. Завершить AI-сессию

```bash
aiw finish ACME-1234
```

Команда должна:

1. повторно запустить artifact scan;
2. зафиксировать количество изменённых путей из `git status` клиентского checkout;
3. сохранить в AI-repo session summary без client code/prompts;
4. записать task ID, tool, роль, workflow, время и количество изменённых путей;
5. удалить runtime/injected instructions согласно retention;
6. оставить commit/push человеку.

Допустимый session summary:

- task ID;
- дата;
- роли агентов;
- tool, role и workflow;
- изменённые типы компонентов, но не исходный код;
- delivery hygiene result;
- lessons learned без клиентских секретов.

Результат: временный контекст очищен, накоплено безопасное процессное знание.

### Шаг 31. Человек создаёт commit и PR

В client-repo:

```bash
cd ../client-product
git status
git diff --check
git diff
git add <EXPLICIT_FILE_LIST>
git diff --cached
git commit -m "ACME-1234: <human-written description>"
git push -u origin feature/ACME-1234-short-description
```

Не используйте `git add .` для первой проверки. Добавляйте явный список файлов.

PR содержит:

- цель;
- фактические изменения;
- выполненные проверки;
- риски и rollback;
- ссылку на task/spec/ADR;
- никаких AI-signatures.

Проверка: клиентский reviewer видит обычный понятный PR, который соответствует правилам проекта.

Результат: feature доставлена без AI-operational artifacts.

## 9. Настройка процессов для следующих участников

### Шаг 32. Onboarding нового разработчика

Дайте разработчику checklist:

1. Получить доступ к обоим репозиториям независимо.
2. Настроить корпоративный Git identity и MFA/SSO.
3. Установить утверждённый AI tool через корпоративный канал.
4. Клонировать оба репозитория соседними каталогами.
5. Выполнить `npm install -g .` и `aiw register .`.
6. Выполнить `aiw doctor` и установить local hook через `aiw install-hooks`.
7. При необходимости установить Codex skill и Claude MCP по разделу 7.
8. Пройти упражнение с synthetic task.
9. Продемонстрировать, что test AI artifact блокируется scanner.
10. Подтвердить data policy и incident path.

Проверка: onboarding считается завершённым только после практического упражнения.

### Шаг 33. Offboarding

1. Удалить человека из private AI-repo.
2. Отозвать AI enterprise seat и active sessions.
3. Отозвать Git/SSH/API tokens.
4. Удалить project-specific credentials с managed device.
5. Проверить private forks и local retention по политике компании.
6. Зафиксировать дату и исполнителя.

Результат: бывший участник не может получить новый доступ к AI-workspace или клиентскому коду.

### Шаг 34. Еженедельная проверка пилота

AI-интегратор собирает:

- количество завершённых и принятых задач;
- cycle time с учётом review/rework;
- размер PR;
- дефекты и rollback;
- rejected/corrected AI outputs;
- AI cost на принятое изменение;
- policy violations;
- artifact leakage events;
- developer feedback.

Не используйте количество prompts/tokens/lines как показатель продуктивности и не применяйте индивидуальную telemetry для performance rating.

Результат: решение о пользе AI основано на поставленном результате.

### Шаг 35. Ежемесячное обновление AI-repo

1. Проверить версии tool/adapters.
2. Повторно проверить vendor privacy/retention settings.
3. Удалить устаревшие агенты и дублирующиеся инструкции.
4. Обновить project commands при изменении CI.
5. Проверить allow/deny rules.
6. Запустить golden tasks/evals.
7. Просмотреть доступ участников.
8. Проверить отсутствие клиентского кода и secrets во всей Git history AI-repo.
9. Создать versioned change через PR.

Результат: AI-workspace развивается контролируемо и не становится свалкой prompts.

## 10. Дообучение project skills на результатах работы

### Что здесь означает «дообучение»

Starter Kit не изменяет веса Codex/Claude и не ведёт скрытую автономную память. Дообучается проектный операционный слой:

- роли агентов;
- skills и conditional references;
- workflow и human gates;
- project profile/glossary;
- output templates;
- adapters, launcher и deterministic checks;
- behavioral evals.

Все изменения хранятся в Git приватного AI-repo, проходят review и могут быть отменены. Client repo остаётся без AI-operational artifacts.

### Шаг 36. Зафиксировать плохой результат

Создавайте improvement record, если агент:

- выбрал неправильный этап/роль;
- придумал отсутствующее требование;
- проигнорировал проектный паттерн;
- выбрал неподходящую test boundary;
- выдал непроверенное утверждение;
- пропустил реальный human gate;
- остановился там, где мог безопасно продолжить;
- повторяет одну и ту же материальную ошибку;
- нарушил repository/data policy либо launcher не применил правило.

Не меняйте skill только потому, что не понравился стиль ответа. Нужен наблюдаемый decision failure, влияние и гипотеза улучшения.

Создайте запись по `evals/templates/failure-record.md`. Она должна быть synthetic или достаточно обезличенной. Запрещены client source, ticket copy, raw prompt/transcript, logs с клиентскими данными, secrets, production/personal data и уникальные client identifiers.

### Шаг 37. Определить правильный слой исправления

| Наблюдаемая причина | Где исправлять |
|---|---|
| неверный path/remote/команда/stack fact | `project/profile.json` или glossary |
| роль выполняет чужую ответственность | `agents/` |
| неверное решение для класса feature/bug/test/docs | соответствующий skill/reference |
| пропущен порядок или gate | `workflows/` |
| системно не хватает поля результата | `templates/` |
| правило не было загружено/применено | adapter/launcher |
| ручная проверка должна быть детерминированной | script/scanner |
| неизвестное product behavior | human decision, не skill |
| разовый tool outage/ошибка выполнения | возможно, AIW менять не нужно |

Не дублируйте одно исправление во всех слоях. Выберите primary cause и минимальную точку изменения.

### Шаг 38. Создать regression eval

По `evals/templates/golden-case.md` создайте behavioral case:

1. минимальный synthetic context;
2. реалистичный request без подсказки правильного ответа;
3. ожидаемые decisions;
4. недопустимые decisions;
5. допустимая вариативность реализации/формулировок;
6. evidence-based score.

Проверьте case на текущей версии AIW. Он должен демонстрировать нужную проблему по правильной причине. Exact wording и заголовки не являются качественной проверкой, если это не machine-readable contract.

### Шаг 39. Запустить improvement session

Из зарегистрированного client или AI-repo:

```text
aiw improve AIW-001 --tool codex
```

Или:

```text
aiw improve AIW-001 --tool claude
```

Команда:

1. проверяет зарегистрированную пару;
2. фиксирует status и HEAD client repo;
3. создаёт временные improvement instructions;
4. запускает tool с приватным AI-repo как рабочей областью;
5. загружает `project-skill-improvement` и его references;
6. после работы проверяет, что client status и HEAD не изменились;
7. запускает structural skill validation;
8. удаляет runtime и показывает AI-repo diff;
9. не выполняет commit/push.

Improvement session поддерживает native mode. Если выбранный security regime разрешает только Docker/VM, не используйте команду до появления утверждённого изолированного improvement adapter; проведите изменение вручную в разрешённой среде.

### Шаг 40. Проверить изменение

Обязательны:

1. failing case теперь проходит по причине, указанной в hypothesis;
2. соседние golden cases не ухудшились;
3. универсальный skill проверен минимум на разных применимых archetypes, например backend и mobile либо frontend и data;
4. skill правильно выбирается и не перехватывает соседние task classes;
5. агент сохраняет автономию в stack-specific технике;
6. не добавились лишние вопросы/refusals;
7. scope, human gates и data policy не ослаблены;
8. `aiw self-test` проходит;
9. для сложного/high-risk изменения выполнен независимый forward test и human review.

### Шаг 41. Выпустить и распространить улучшение

1. Покажите reviewer failure classification, hypothesis, AIW diff и before/after/adjacent evals.
2. Создайте commit/PR только в приватном AI-repo.
3. Не включайте raw client evidence.
4. Запишите rollback commit/version.
5. После merge команда выполняет:

```text
git pull --ff-only
npm install -g .
aiw register .
aiw desktop-install codex       # если изменился установленный Codex skill/launcher
```

Claude local MCP указывает на AI-repo path и получает обновлённый launcher после `git pull`; при packaged `.mcpb` нужно выпустить и установить новую проверенную версию extension.

6. Наблюдайте следующие применения. Если исходная ошибка сохраняется или соседние задачи ухудшились, refine/revert вместо добавления ещё одного конфликтующего правила.

### Governance learning loop

Для каждого проекта назначьте:

- владельца failure triage;
- человека, разрешающего обезличивание реального инцидента;
- reviewer skills/workflows;
- владельца eval baseline;
- release/rollback owner AI-repo.

На ежемесячном review удаляйте дублирующиеся/устаревшие инструкции, а не только добавляйте новые. Оценивайте принятие результата, rework, escaped defects, policy violations и качество решений; не оптимизируйте prompts/tokens/lines of code.

## 11. Критерии готовности интеграции

Интеграция считается завершённой только если выполнены все пункты:

- [ ] договорное решение задокументировано;
- [ ] data lane выбран;
- [ ] approved AI tool настроен корпоративно;
- [ ] private AI-repo создан и защищён;
- [ ] доступ имеет только проектная команда;
- [ ] client-repo и AI-repo клонируются соседними каталогами;
- [ ] profile содержит точный client remote;
- [ ] launcher отклоняет неверный remote;
- [ ] permissions реально применяются adapter/sandbox, а не только записаны в JSON;
- [ ] проект зарегистрирован в локальном AIW registry;
- [ ] `aiw` определяется из client repo;
- [ ] выбранные Desktop-интеграции проверены;
- [ ] agent roles и workflows заполнены;
- [ ] artifact scanner ловит тестовый запрещённый файл;
- [ ] pre-push hook установлен и проверен;
- [ ] client-repo не содержит AI-config, submodule или ссылок на private AI-repo;
- [ ] AI-repo не содержит копии клиентского кода;
- [ ] первая synthetic feature прошла полный flow;
- [ ] первая реальная feature принята human reviewer;
- [ ] runtime cleanup проверен;
- [ ] incident и offboarding процедуры назначены;
- [ ] baseline metrics собраны.
- [ ] определён владелец skill feedback/eval/release loop;
- [ ] команда знает, что learning records не содержат client artifacts;
- [ ] `project-skill-improvement` и шаблоны evals проверены;

## 12. Порядок выполнения без объяснений

Для использования как короткого operational checklist:

1. Назначить ответственных.
2. Проверить договор.
3. Выбрать Green/Amber/Red lane.
4. Утвердить AI tool и enterprise settings.
5. Создать private `<project>-ai-workspace` на GitHub/GitLab/Bitbucket.
6. Ограничить доступ проектной командой.
7. Защитить main.
8. Клонировать AI-repo и развернуть в него Starter Kit.
9. Клонировать client-repo соседним каталогом.
10. Заполнить `project/profile.json` точным path/remote/branch/data lane/tool/commands.
11. Проверить `permissions.json` и `forbidden-artifacts.json`.
12. При желании передать агенту `SETUP-PROJECT-WITH-AGENT-RU.md`.
13. Выполнить `npm install -g .`.
14. Выполнить `aiw register .` и `aiw projects`.
15. Выполнить `aiw self-test` и `aiw doctor`.
16. Установить pre-push hook через `aiw install-hooks`.
17. Проверить `BLOCK` на временном AI-файле и финальный `PASS`.
18. Установить Codex skill, если команда использует Codex Desktop.
19. Подготовить/установить Claude MCP, если команда использует Claude Desktop.
20. Сделать PR и утвердить первую проектную версию AI-repo.
21. Выполнить synthetic feature end-to-end.
22. Исправить найденные проблемы.
23. Выполнить первую реальную feature через feature flow.
24. Проверить diff и выполнить human commit/push/PR.
25. Выполнить `aiw finish <task-id>`.
26. Измерить полный цикл до принятия PR.
27. Через 4–8 недель сравнить с baseline.
28. Масштабировать только полезные use cases.
29. Завести sanitized failure record при материальной ошибке агента.
30. Создать failing behavioral eval.
31. Запустить `aiw improve AIW-<number>`.
32. Выполнить before/after и adjacent regression evals.
33. Выпустить AIW-изменение через human-reviewed PR и сохранить rollback.

## 13. Статус reference Starter Kit и проектные переменные

Reference Starter Kit уже содержит исполняемые launcher, registry wrapper, scanner, hook installer, Codex skill installer, Claude local MCP server, Docker adapter, роли, skills и workflows.

Для каждого нового проекта не переписывайте launcher. Настройте только проектные переменные:

1. project ID и безопасное display name;
2. относительный путь и точные remotes client repo;
3. default branch;
4. согласованный data lane;
5. default tool и утверждённые модели;
6. native или Docker mode;
7. stack-specific install/format/lint/typecheck/test/build commands;
8. project-specific risks, glossary, roles и workflow additions;
9. необходимость Codex Desktop, Claude Desktop и external required status check.

Reference-конфигурация поддерживает Codex и Claude Code, macOS и Windows, GitHub/GitLab/Bitbucket, native и Docker modes. Client Git platform не влияет на локальную связь: launcher проверяет фактический Git remote.

Для ручной настройки используйте разделы 4–7. Для настройки агентом передайте ему `project-ai-workspace-template/SETUP-PROJECT-WITH-AGENT-RU.md`. После настройки AI-integrator обязан просмотреть AI-repo diff и результаты отрицательного scanner test до merge первой версии AI-repo.
